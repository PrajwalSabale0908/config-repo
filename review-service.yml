server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/review_db
    username: root
    password: Prajwal@09

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
